export enum VaultStatus {
    UPCOMING = 'UPCOMING',
    LIVE = 'LIVE',
    SUSPENDED = 'SUSPENDED',
    INVALID = 'INVALID',
}

export enum PendingWithdrawStatus {
    NONE = 0,
    PENDING = 1,
    WAITING = 2,
    READY = 3,
}
